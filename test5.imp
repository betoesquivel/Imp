main() {
  int b = 8;
 int x, y, z;

 z = b * 1;

  x = b * 3 * 4 / 4;
  int a = 2 / 3 * x;
	if (a > b+a/2*5/8) {
	x=a;
	  if (b<a){
	  x=2;
	  }
	  else {
	  x=3*5;
          }		
	}
	else {
	x=b;
	}
  b = a+2*4/2;		
}
